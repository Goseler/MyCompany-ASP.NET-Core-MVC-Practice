@model ServiceItem
@{
    ViewBag.Title = Model.MetaTitle;
    ViewBag.Description = Model.MetaDescription;
    ViewBag.KeyWords = Model.MetaKeywords;
}

<div>
    <h1>@Model.Title</h1>
    <h2>@Model.Subtitle</h2>
    <div>
        <img src="~/images/uploads/@Model.TitleImagePath" class="img-block"/>
    </div>
</div>
<div>
    @Html.Raw(Model.Text)
</div>